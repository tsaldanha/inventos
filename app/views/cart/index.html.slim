h1 Carrinho
- unless @items.empty? 
    == form_for :order, :url => new_order_path(), method: "get" do

        - @items.each do |item|
            div
                h2 = item.name
                span = format_currency(item.price)
                = link_to "Delete", cart_item_path(item.id), method: :delete
        
        div
            span == format_currency(@total)
        div
            button#addToCart.btn.btn-primary Finalizar Reserva    
            
- else
    p 
    | nenhum produto encontrado
